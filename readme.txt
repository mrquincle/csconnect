First install something like

  sudo apt-get install libcurl-dev liboauth-dev

And Ubuntu curl is a virtual package, so you will need:

  sudo apt-get install libcurl4-openssl-dev

Or libcurl4-nss-dev, libcurl4-gnutls-dev 
